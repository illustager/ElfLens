<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ElfLens.Core.ViewModels"
             x:Class="ElfLens.Views.FileInfoPanelView"
             x:DataType="vm:FileInfoPanelViewModel"
             x:Name="root">

    <ScrollViewer>
        <StackPanel Spacing="6" Margin="6">

            <Button Content="Refresh" Command="{Binding RefreshCommand}"
                    Padding="8,2" FontSize="11" HorizontalAlignment="Left"
                    IsEnabled="{Binding IsBusy, Converter={x:Static BoolConverters.Not}}" />

            <!-- File Type -->
            <Border Padding="6,4" CornerRadius="4"
                    Background="{DynamicResource SystemAltMediumLowBrush}">
                <StackPanel Spacing="2">
                    <SelectableTextBlock Text="FILE TYPE" FontSize="11" FontWeight="Bold"
                               Foreground="{DynamicResource SystemBaseHighColorBrush}" />
                    <SelectableTextBlock Text="{Binding FileType}" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <!-- Security -->
            <Border Padding="6,4" CornerRadius="4"
                    Background="{DynamicResource SystemAltMediumLowBrush}">
                <StackPanel Spacing="2">
                    <ToggleButton x:Name="secuToggle" Content="SECURITY" FontSize="11"
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                  Background="Transparent" BorderThickness="0"
                                  HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                  Padding="0" Cursor="Hand" />
                    <ItemsControl ItemsSource="{Binding SecurityInfo}"
                                  IsVisible="{Binding #secuToggle.IsChecked}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:SecurityItem">
                                <Grid ColumnDefinitions="14,*,Auto" MinHeight="20">
                                    <SelectableTextBlock Grid.Column="0" FontSize="13" VerticalAlignment="Center"
                                               Text="✗" Foreground="#F44336"
                                               IsVisible="{Binding Enabled, Converter={x:Static BoolConverters.Not}}" />
                                    <SelectableTextBlock Grid.Column="0" FontSize="13" VerticalAlignment="Center"
                                               Text="✓" Foreground="#4CAF50"
                                               IsVisible="{Binding Enabled}" />
                                    <SelectableTextBlock Grid.Column="1" Text="{Binding Name}"
                                               FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" />
                                    <SelectableTextBlock Grid.Column="2" Text="{Binding Detail}"
                                               FontSize="10" VerticalAlignment="Center" Opacity="0.6" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- ELF Header -->
            <Border Padding="6,4" CornerRadius="4"
                    Background="{DynamicResource SystemAltMediumLowBrush}">
                <StackPanel Spacing="2">
                    <ToggleButton x:Name="hdrToggle" Content="ELF HEADER" FontSize="11"
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                  Background="Transparent" BorderThickness="0"
                                  HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                  Padding="0" Cursor="Hand" />
                    <ItemsControl ItemsSource="{Binding ElfHeaders}"
                                  IsVisible="{Binding #hdrToggle.IsChecked}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:KeyValueItem">
                                <Grid ColumnDefinitions="140,*" MinHeight="18">
                                    <SelectableTextBlock Grid.Column="0" Text="{Binding Key}"
                                               FontSize="11" Opacity="0.7" VerticalAlignment="Center" />
                                    <SelectableTextBlock Grid.Column="1" Text="{Binding Value}"
                                               FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"
                                               Margin="12,0,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Section Table -->
            <Border Padding="6,4" CornerRadius="4"
                    Background="{DynamicResource SystemAltMediumLowBrush}">
                <StackPanel Spacing="2">
                    <ToggleButton x:Name="secToggle" Content="SECTIONS" FontSize="11"
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                  Background="Transparent" BorderThickness="0"
                                  HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                  Padding="0" Cursor="Hand" />
                    <Grid IsVisible="{Binding #secToggle.IsChecked}">
                        <StackPanel Spacing="4">
                            <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto" Margin="0,0,0,4"
                                  x:CompileBindings="False">
                                <SelectableTextBlock Grid.Column="0" Text="Name" FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                           Width="{Binding ColNameWidth}" />
                                <SelectableTextBlock Grid.Column="1" Text="Type" FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                           Width="{Binding ColTypeWidth}" />
                                <SelectableTextBlock Grid.Column="2" Text="Address" FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                           Width="{Binding ColAddrWidth}" />
                                <SelectableTextBlock Grid.Column="3" Text="Size" FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                           Width="{Binding ColSizeWidth}" />
                                <SelectableTextBlock Grid.Column="4" Text="Flags" FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource SystemBaseHighColorBrush}"
                                           Width="{Binding ColFlagsWidth}" />
                            </Grid>
                            <ItemsControl ItemsSource="{Binding Sections}" x:CompileBindings="False">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,Auto" MinHeight="18">
                                            <SelectableTextBlock Grid.Column="0" Text="{Binding Name}" FontSize="11"
                                                       FontFamily="Cascadia Code, Consolas, monospace"
                                                       Width="{Binding DataContext.ColNameWidth, ElementName=root}" />
                                            <SelectableTextBlock Grid.Column="1" Text="{Binding Type}" FontSize="10" Opacity="0.8"
                                                       Width="{Binding DataContext.ColTypeWidth, ElementName=root}" />
                                            <SelectableTextBlock Grid.Column="2" Text="{Binding Address}" FontSize="10"
                                                       FontFamily="Cascadia Code, Consolas, monospace" Opacity="0.8"
                                                       Width="{Binding DataContext.ColAddrWidth, ElementName=root}" />
                                            <SelectableTextBlock Grid.Column="3" Text="{Binding Size}" FontSize="10" Opacity="0.8"
                                                       Width="{Binding DataContext.ColSizeWidth, ElementName=root}" />
                                            <SelectableTextBlock Grid.Column="4" Text="{Binding Flags}" FontSize="10"
                                                       FontFamily="Cascadia Code, Consolas, monospace" Opacity="0.8"
                                                       Width="{Binding DataContext.ColFlagsWidth, ElementName=root}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
